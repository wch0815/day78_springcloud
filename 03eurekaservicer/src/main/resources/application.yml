server:
  port: 10000
eureka:
  client:
    service-url:
      defaultZone: http://localhost:20000/eureka,http://localhost:30000/eureka
#    register-with-eureka: false
#    fetch-registry: false
spring:
  application:
    name: eureka-server